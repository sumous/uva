#include<stdio.h>
#include<string.h>
#define M 10005
struct node
{
	char c;
	struct node* next,*pre;
};
char s[M];
int main()
{
	int n;
	scanf("%d",&n);
	while(n--)
	{
		gets(s);
	}
	return 0;
}